<h4>Cat Categry<h4>
  <%= link_to "Browse by Dog", dog_path %>
  <%= link_to "Browse by Cat", cat_path %>
  <%= link_to "Browse by Other", other_path %>
  <% @products = Products.all %>
  <% @products.each do |product| %>
  <% end %>
